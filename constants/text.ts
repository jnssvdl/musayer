import { StyleSheet } from "react-native";

const text = StyleSheet.create({
  large: {
    fontSize: 16,
    fontWeight: "bold",
    color: "#f4f4f5",
  },
  medium: {
    fontSize: 14,
    color: "#71717a",
  },
  small: {
    fontSize: 14,
    color: "#fafafa",
  },
});

export default text;
